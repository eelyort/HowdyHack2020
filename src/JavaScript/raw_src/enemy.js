class Enemy extends{
    constructor(startX, startY, range, angle) {
        this.x = startX;
        this.y = startY;
        this.direction = 0; // N/E/S/W = 0/1/2/3
    }
    calculateDamage(xIn, yIn){

    }
}